name: mistake_book
description: Offline Error Tracker App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.3+1
  path_provider: ^2.1.3
  path: ^1.9.0
  receive_sharing_intent: ^2.0.0
  image_picker: ^1.1.2

flutter:
  uses-material-design: true
